package dev.tbm00.preprocessit.model.data.enums;

public enum Action {
    TRY_NEIGHBORS,
    SHIP,
    EXIT_TO_NEXT_ATTRIBUTE_ITERATION,
    EXIT_TO_NEXT_TOKEN_ITERATION,
    CONTINUE,
    TRIM_MATCH_ALL,
    TRIM_MATCH_FIRST,
    TRIM_MATCH_FROM_START,
    TRIM_MATCH_FROM_END,
    KEEP_MATCH,
    APPEND,
    PREPEND,
    INSERT_AT
}